import React from 'react';

const Header = (props) => {
    return (
        <header>
            <i className="fas fa-cubes"></i>
            <h1>WorldTick</h1>
        </header>
    );
}

export default Header